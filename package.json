{
  "name": "startup-markets",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev --host",
    "build": "astro build",
    "preview": "astro preview --host"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "astro": "^5.14.1"
  }
}
